[project]
name = "lithium-project25"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
